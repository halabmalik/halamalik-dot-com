<!DOCTYPE html>
<html>
<head>
    <title>Posts tagged "{{ tag }}"</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    {% include "nav.njk" %}
    <div class="container">
        <main class="post-content">
            <h1 class="post-title">Posts tagged "{{ tag }}"</h1>
            <div class="post-list">
                {% for post in collections[tag] | reverse %}
                <div class="post-item">
                    <a href="{{ post.url }}">{{ post.data.title }}</a>
                    {% if post.data.date %}
                    <div class="post-date">{{ post.data.date | date }}</div>
                    {% endif %}
                </div>
                {% endfor %}
            </div>
        </main>
    </div>
</body>
</html> 